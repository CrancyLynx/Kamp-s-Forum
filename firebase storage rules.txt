rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn() && 
             firestore.get(/databases/(default)/documents/kullanicilar/$(request.auth.uid)).data.role == 'admin';
    }

    function isValidImage() {
      return request.resource.contentType.matches('image/.*')
             && request.resource.size < 10 * 1024 * 1024;
    }

    // --- KURALLAR ---

    // Profil Resimleri
    match /profil_resimleri/{userId}/{fileName} {
      allow read: if true;
      allow write: if (isSignedIn() && request.auth.uid == userId && isValidImage()) || isAdmin();
      allow delete: if (isSignedIn() && request.auth.uid == userId) || isAdmin();
    }

    // Gönderi, Yorum, Ürün vb. (Klasör/UserID/Dosya yapısı)
    match /{folder}/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if (isSignedIn() && request.auth.uid == userId && isValidImage()) || isAdmin();
      allow delete: if (isSignedIn() && request.auth.uid == userId) || isAdmin();
    }

    // Sohbet Resimleri (DÜZELTİLEN KISIM)
    // Eski kural firestore.get ile sohbeti kontrol ediyordu, bu da ilk mesajda hata veriyordu.
    // Şimdi sadece giriş yapmış olma şartı aranıyor.
    match /chat_images/{chatId}/{fileName} {
      allow read: if isSignedIn();
      allow write: if isSignedIn() && isValidImage();
    }
  }
}